# Phase 6: 设备地图 实施计划

**Goal:** Leaflet.js 柬埔寨设备地图，颜色标注状态，图层切换，搜索定位

**Tasks:**
1. API: `GET /api/devices/geo` — return device list with GPS + status
2. UI: New "设备地图" tab — Leaflet map + markers + layer filter + search
3. Regression + smoke
